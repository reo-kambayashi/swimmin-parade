<section class="relative overflow-hidden py-16 sm:py-28">
  <div class="pointer-events-none absolute inset-0" aria-hidden="true">
    <div class="contact-gradient absolute -top-20 left-1/2 h-64 w-[520px] -translate-x-1/2 rounded-[45%] bg-gradient-to-r from-sky-400/35 via-indigo-400/20 to-blue-500/25 blur-3xl"></div>
    <div class="contact-orbit absolute bottom-12 right-12 hidden h-40 w-40 rounded-full border border-white/20 sm:block"></div>
  </div>
  <div class="mx-auto max-w-3xl px-4 text-center">
    <h2 class="mb-6 text-3xl font-bold font-heading text-text sm:text-4xl" data-animate="fade-up" data-animate-distance="34px">
      Contact Us
    </h2>
    <p class="mx-auto mb-8 max-w-2xl text-base leading-relaxed text-text/80 sm:text-lg" data-animate="fade-up" data-animate-delay="0.08s">
      取材・出演依頼など、お気軽にご連絡ください。
    </p>
    <a
      href="/contact"
      class="btn-pill btn-float focus-ring w-full max-w-[320px] bg-gradient-to-r from-blue-600 to-sky-500 py-4 text-lg text-white shadow-pop duration-300 hover:shadow-[0_18px_40px_rgba(37,99,235,0.28)]"
      data-animate="scale-fade"
      data-animate-delay="0.14s"
      data-animate-distance="26px"
    >
      お問い合わせ
    </a>
  </div>
</section>

<style>
  .contact-gradient {
    animation: contactWave 12s ease-in-out infinite alternate;
    opacity: 0.8;
  }

  .contact-orbit {
    background: conic-gradient(from 120deg, rgba(255, 255, 255, 0.12), transparent 65%);
    border: 1px solid rgba(59, 130, 246, 0.18);
    animation: orbitSpin 10s linear infinite;
    opacity: 0.75;
  }

  @keyframes contactWave {
    0% {
      transform: translate3d(-20%, 12%, 0) rotate(-6deg) scale(0.92);
    }
    50% {
      transform: translate3d(12%, -8%, 0) rotate(2deg) scale(1.04);
    }
    100% {
      transform: translate3d(20%, 4%, 0) rotate(6deg) scale(0.9);
    }
  }

  @keyframes orbitSpin {
    0% {
      transform: rotate(0deg) scale(0.94);
    }
    100% {
      transform: rotate(360deg) scale(1.06);
    }
  }
</style>
