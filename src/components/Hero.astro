---
---
<section class="relative h-[calc(100vh-5rem)] flex items-center justify-center text-white overflow-hidden">
  <div class="absolute inset-0 bg-primary/80 z-10"></div>
  <div class="absolute inset-0">
    <img src="/images/hero-background.jpg" alt="Band photo" class="w-full h-full object-cover">
  </div>
  <div class="relative z-20 text-center px-4">
    <h1 class="text-5xl md:text-7xl font-bold font-heading mb-4 animate-fade-in-up">swimmin' parade</h1>
    <p class="text-xl md:text-2xl mb-8 animate-fade-in-up animation-delay-300">都市のノイズを、光に変える。</p>
    <a href="/releases#latest" class="bg-gradient-to-r from-blue-600 to-sky-500 text-white font-bold py-3 px-8 rounded-full text-lg hover:scale-105 transition-transform duration-300 animate-fade-in-up animation-delay-600">
      最新リリースを聴く
    </a>
  </div>
</section>

<style>
  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  @keyframes fade-in-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animation-delay-300 {
    animation-delay: 0.3s;
  }

  .animation-delay-600 {
    animation-delay: 0.6s;
  }
</style>
